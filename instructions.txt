cd ~/Desktop/ai_app
npm run dev

For dataset purposes only:

-- See all registered patients
SELECT id, full_name, created_at FROM patients;

-- See all screening results
SELECT p.full_name, s.screening_type, s.result, s.confidence, s.created_at 
FROM screening_history s 
JOIN patients p ON s.patient_id = p.id 
ORDER BY s.created_at DESC;

-- Exit when done
.quit


Manage access;
sqlite3 ~/ddart-backend/patients.db
SELECT id, full_name, email, created_at FROM doctors WHERE approved = 0;
UPDATE doctors SET approved = 1 WHERE email = 'doctor@email.com';
.quit